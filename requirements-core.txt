# ctp-core (open / reproducible core) runtime dependencies — IORN-001
# Excludes the GUI (tkinter) and DICOM input/output (pydicom).
# For the dependencies of the whole application, see requirements.txt.
numpy>=1.21.0
scipy>=1.7.0
matplotlib>=3.5.0
